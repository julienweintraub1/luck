name: nfl_players_app
description: A new Flutter project.

environment:
  sdk: '>=3.2.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.0.6

dev_dependencies:
  flutter_lints: ^1.0.0
  flutter_test:
    sdk: flutter
  mockito: ^5.0.0 
  
flutter:
  uses-material-design: true
  assets:
    - assets/player.json
